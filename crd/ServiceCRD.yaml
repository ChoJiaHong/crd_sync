
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: services.ha.example.com
spec:
  group: ha.example.com
  names:
    plural: services
    singular: service
    kind: Service
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:                                    # ← 改成 spec
              type: object
              x-kubernetes-preserve-unknown-fields: true
          # （可選）之後再逐步補更嚴謹的 schema
